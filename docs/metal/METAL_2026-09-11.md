# METAL 2026-09-11 -- la semana de los vatios, y nada de esto lo ha ejecutado un CPU

> La hoja de la tanda. Misma regla que las diez anteriores, y es la que hace que
> esto no sea *"a ver que pasa"*:
>
> > **Cada prueba dice QUE AFIRMA y COMO SE CAE.** Una prueba que solo puede
> > salir bien no prueba nada: si no se sabe de antemano que aspecto tiene el
> > fallo, cualquier cosa que aparezca en pantalla se lee como exito.
>
> Y la de orden: **lo que no toca nada va primero; lo que no se deshace, al
> final.**

---

# 0. QUE HAY AQUI SIN PROBAR, y por eso esta hoja es larga

Desde la hoja del 10-09 entraron ocho commits y **ninguno ha corrido en el
Ryzen**. Ordenados por lo que pueden romper:

```text
   8eba6691  [consumo] + TRES CORTES de Ring 0      <- lo que puede no arrancar
             core/shell/ui.rs        -> espera.rs   la espera del shell
             plat/smp/crew.rs        -> obrero.rs   el bucle del obrero
             task/scheduler/roja.rs  -> dormir.rs   la tarea idle
   366bf9b0  W1 el BSP duerme hondo, W5 DOOM duerme, W4 reposo del escritorio
   54fa9101  W4b el reposo no entraba nunca
   6ca050c5  R-APP8 lo que no se ve, no se pinta    <- DOOM y ray.bex
   0a328dc5  DOOM en una VENTANA (V1 de la hoja anterior)
   512cef4a  a82c / a84c: documentos y reparto de tablas, no ejecutan
   3a7df6b5  PANTALLA COMPLETA en caliente (Alt+Enter) + `Vista::Tapada`
   47e85d26  una fuente para las APPS (`fuente.h`)   <- `fontgen` regenera Ring 0
   87e714d3  la LETRA llega a la app (bit 62)        <- toca el reparto de teclas
   46baefd8  `texto.bex`, el bloc de notas
```

★ `47e85d26` toca dos ficheros de Ring 0 --los genera-- y se comprobo que salen
BYTE A BYTE iguales. O sea que para el kernel ese commit no existe; si el
arranque falla, no es ese.

★★ `87e714d3` si cambia el camino por el que pasa CADA TECLA del escritorio. Es
el que hay que mirar si la linea de Ejecutar deja de escribir.

★ Los tres cortes mueven codigo de Ring 0 de sitio. Se demostro que **etiquetar
no cambio un byte del `.text`**, pero los cortes si mueven simbolos, y eso solo
lo dice el metal. Si el arranque falla, el sospechoso esta en esa lista de tres
y en ningun otro sitio.

---

# 1. EL ORDEN, de un vistazo

```text
   A  ARRANCA?          los tres cortes. Si no, lo de abajo no importa
   B  NO TOCA NADA      consumo, save, zzz, banda
   C  CAMBIA ESTADO     smp all, DOOM en ventana, minimizar
   D  NO SE DESHACE     `texto.bex` guardando: es la PRIMERA escritura a
                        disco que no la hace un programa de prueba
```

---

# 2. A -- ARRANCA, Y LOS TRES CORTES

## A1 -- El shell escribe: la ESPERA salio de `ui.rs`

**Que afirma**: que `core/shell/espera.rs` --la cabeza de la vuelta de
`shell_read_line`, movida entera-- sigue leyendo el teclado y pintando CABINA.

```text
   teclear `help` en el shell de Ring 0, y que salga la ayuda
```

| sintoma | que significa |
|---|---|
| el shell no responde al teclado | el bucle ya no llama a la espera. Es el corte, y se deshace devolviendo `siguiente_byte` a `ui.rs` |
| responde pero CABINA no se pinta | el `render_hud` se quedo fuera de la vuelta |
| responde y CABINA se pinta | el corte esta bien. Sigue |

## A2 -- Los obreros: el bucle salio de `crew.rs`

**Que afirma**: que `plat/smp/obrero.rs` --la funcion entera, con los `static`
del reparto en `pub(super)`-- sigue entrando y repartiendo.

```text
   smp all      ->  12 de 12
   smp prueba   ->  una aceleracion (el 24-08 dio 11,59x)
   smp          ->  la tabla, con la fila `bsp` y el `zzz`
```

| sintoma | que significa |
|---|---|
| `atriles que tocaron` menor que los pedidos | un obrero no llego al bucle: el corte se llevo algo del camino |
| `smp prueba` ~0,99x con los doce en pie | entraron y no vieron la ronda: la publicacion de las atomicas |
| la maquina se queda al teclear `smp all` | el trampolin o la pila, y entonces NO es este corte |

## A3 -- La tarea idle vive ahora en `dormir.rs`

**Que afirma**: que `dormir::idle_thread` --la misma que estaba en el
planificador-- sigue siendo la que corre cuando no hay nada que hacer.

```text
   consumo   ->  fila `bsp`: dormido hondo ____ ms = ____ % en ____ siestas
```

| sintoma | que significa |
|---|---|
| `bsp dormido 0 ms` | la tarea idle no corre, o `se_puede()` dijo que no. Mirar `zzz=` en CABINA |
| `bsp en hlt (C1): sin MONITORX` | el silicio no lo trae. No deberia: este Zen 3 si |
| un numero y un porcentaje | el corte esta bien |

---

# 3. B -- W0: LOS VATIOS, sin tocar nada

## 3.1 Los tres numeros

```text
   consumo (shell, DOS veces seguidas)     gasta paquete ____ W
   save (escritorio, 2 min quieto)         gasta paquete ____ W
   Windows con HWiNFO, 2 min quieto        Package Power ____ W
```

[!] **Windows es REFERENCIA, no suelo ni meta** (corregido el 11-09 a peticion
del propietario). El suelo lo pone el silicio, y BMO-X --que no tiene nada detras--
puede quedar POR DEBAJO. Ver `docs/maestro/EFICIENCIA_MAESTRO.md`, 2.1.

## 3.2 ★ LA PREDICCION, escrita ANTES de arrancar

Lo que la lectura del 11-09 dice que va a pasar. Si sale otra cosa, la lectura
estaba mal, y eso vale mas que el numero:

```text
   zzz=C1                 el CPUID de este Ryzen solo enumera C1 para MWAIT
                          (hoja 5, EDX = 0x11, leido desde Windows en el MISMO
                          chip). O sea que W1 NO gano profundidad
   los vatios bajan POCO  o nada. **Y eso no es un fallo**: es la prueba de que
                          la palanca es W6 --C2 por el puerto de E/S-- y no
                          dormir mas veces en C1
   `consumo` en el shell  mide un BSP que NO descansa: el shell gira mientras
                          el teclado es suyo, y lo dice `espera.rs`. El numero
                          que vale es el del ESCRITORIO
```

| sintoma | que significa |
|---|---|
| `zzz=` dice algo que no es C1 | la lectura del CPUID estaba mal, y W1 si gano profundidad. Apuntarlo: cambia el plan entero |
| vatios iguales y `bsp reposo` alto | lo esperado, y W6 queda justificado con numero |
| vatios MAS altos que los 58,5 W del 26-08 | algo nuevo gira. Mirar `bsp siestas` y el pulso del escritorio |

## 3.3 Y una medida vieja que cuesta una palabra

```text
   banda   ->   el ancho de banda de la memoria, en GB/s
```

**Que afirma**: cual de las tres filas de `PERFIL/RAM.txt` es esta maquina
(3200 / 2666 / 2133 MT/s). La orden existe desde agosto y **nadie la ha
ejecutado nunca**; de ese numero depende el plan del asistente.

---

# 4. B -- W4b: el escritorio en REPOSO

**Que afirma**: que con la maquina quieta el bucle del escritorio baja de mil
vueltas por segundo a ~125, y que la barra lo DICE.

```text
   arrancar el escritorio, NO tocar nada cinco segundos, y mirar la barra
```

| sintoma | que significa |
|---|---|
| el pulso dice `reposo` y ~125/s | W4b hecho: el cuarto de segundo ya no cuenta como actividad |
| dice `latido` y ~1000/s | algo pinta cada vuelta. Si `pinta` sale 4/s, es el cuarto de segundo y el arreglo no entro |
| dice `reposo` y una tecla tarda | esperado hasta 8 ms. Mas que eso es otra cosa |
| la alarma de ritmo bajo encendida | el reposo no deberia dispararla: fallo del letrero, no del reposo |

---

# 5. C -- R-APP8: LO QUE NO SE VE, NO SE PINTA

La idea del propietario, y la primera vez que se puede medir en vatios.

## C1 -- DOOM en una ventana

```text
   doble clic en el icono de DOOM, en el escritorio
```

**Que afirma**: que DOOM pide una superficie de 960x600 con buzon y el DIRECTOR
la compone dentro de su marco. Es V1 de la hoja anterior, y sigue sin probarse.

## C2 -- ★★ MINIMIZAR DOOM: la prueba de los vatios

```text
   con DOOM jugando en su ventana: `save` (apuntar W), minimizarlo,
   esperar 10 s, `save` otra vez (apuntar W)
```

**Que afirma** tres cosas, y cada una se cae distinto:

```text
   1. los vatios BAJAN       DOOM dejo de dibujar 960x600 para nadie
   2. DOOM SIGUE VIVO        al restaurar, el mundo avanzo: la musica sigue
                             donde toca y los monstruos se movieron
   3. NO sale la acusacion   si en la consola aparece
                             "[vista] tid N pinta sin que se le vea",
                             DOOM no esta leyendo la VISTA
```

| sintoma | que significa |
|---|---|
| los vatios no bajan y no hay acusacion | DOOM obedece y el gasto no era el dibujo. Apuntarlo: contradice lo previsto |
| sale la acusacion | el port no lee el byte 2, o el DIRECTOR no lo escribe. Comprobar que la ventana tenia buzon |
| al restaurar, DOOM esta congelado | el juez dijo "no se ve" y nadie lo desdijo: `decidir_vista` no volvio a `SeVe` |
| al restaurar, el mundo esta igual que al minimizar | R-APP8 roto del otro lado: se paro la LOGICA y no solo el dibujo |

## C3 -- Restaurar, y el sacrificio declarado

**Que afirma**: que al restaurar se ve el ultimo fotograma un instante, y el
siguiente ya es de ahora. Es el sacrificio escrito en R-APP8 (L3).

| sintoma | que significa |
|---|---|
| un parpadeo con el fotograma viejo | lo previsto |
| la ventana en negro hasta que DOOM pinte | el `mark_dirty` del retorno no entro |
| se queda con el fotograma viejo para siempre | DOOM no volvio a dibujar: ver C2 |

## C4 -- `ray.bex`, lo mismo y mas barato

```text
   run c/ray.bex   (se abre en ventana), minimizar, restaurar
```

**Que afirma**: que el dibujo entero quedo dentro del `if (se_ve == 1)` y el
bucle sigue leyendo la entrada. Fallo: al restaurar no reacciona a las teclas --
se salto tambien la entrada, que no era la idea.

## C5 -- La pantalla prestada

```text
   con DOOM o ray en ventana, lanzar algo que reclame la pantalla entera
   (`presta`, o un `.bex` con WANTS_SCREEN)
```

**Que afirma**: que mientras la pantalla esta prestada NINGUNA ventana se ve, y
que al volver se ven otra vez. Fallo: al volver las ventanas siguen sin
pintarse -- `prestar(false)` no se llamo en el camino de vuelta.

---

# 5b. ESCRIBIR DENTRO DE UNA VENTANA -- `texto.bex`

Va al final de C a proposito: toca el reparto de teclas del escritorio, y si algo
de aqui sale mal hay que poder descartar todo lo anterior antes.

## T0 -- ★★ LA PRIMERA: que el escritorio siga escribiendo

```text
   arranca, y SIN abrir nada escribe en la caja de Ejecutar
```

Esta es la prueba de no-regresion de `87e714d3`, y va ANTES de probar el bloc.
El cambio esta en el camino por el que pasa cada tecla cocida: si la linea de
Ejecutar no escribe, o escribe letras de mas, el culpable es ese commit y no hay
que seguir.

## T1 -- Abre, y ENSENA TEXTO

```text
   run c/texto.bex
```

```text
   [ ] sale una ventana de 760x500 con su marco del DIRECTOR
   [ ] se LEEN letras: barra de arriba con la ruta, numeros en el canal,
       "lin 1  col 1   N bytes" abajo
   [ ] si `datos/notas.txt` no existe, el aviso dice "nuevo: ... aun no existe"
```

** Esta es la primera vez que una app dibuja TEXTO con su propia fuente. Si sale
basura con forma de letra --glifos del reves, o la letra equivocada-- el
sospechoso es el `[riesgo] ESPEJO` de `fuente.h`: el bit 7 es la columna
izquierda, y el renderer del kernel lee la misma tabla con la misma cuenta.

## T2 -- ★★ SE ESCRIBE: la prueba que no existia

```text
   [ ] letras normales aparecen donde esta el cursor
   [ ] TILDES Y ENE: la tecla de la ene --la de la derecha de la L-- y las
       cinco vocales con tilde (tecla muerta + vocal). Son el motivo entero
       de que el caracter lo mande el kernel y no lo deduzca la app
   [!] Y el guardian `ascii-sweep` cazo esta misma casilla en su primera
       version, porque la escribi con una palabra que sin su ene dice otra
       cosa. La regla funciona hasta en la hoja que la prueba
   [ ] retroceso borra hacia atras, Supr hacia delante
   [ ] retorno parte la linea, y el numero del canal sube
   [ ] flechas, Inicio, Fin, RePag, AvPag mueven el cursor
   [ ] ** ARRIBA Y ABAJO CONSERVAN LA COLUMNA (y la recortan en una linea corta)
   [ ] un clic dentro del texto pone el cursor donde cayo el dedo
```

[!] Y lo que NO tiene que pasar: que una tecla cuente DOS VECES. Seria que algo
esta leyendo las dos colas --la cruda y la cocida-- para lo mismo.

## T3 -- GUARDA, y es la primera escritura de verdad

```text
   [ ] clic en [Guardar] -> el aviso dice "guardado" y el punto naranja
       de la barra de arriba DESAPARECE
   [ ] clic en [Recargar] -> vuelve lo guardado
   [ ] cierra por el boton del marco, vuelve a abrir: el texto sigue ahi
```

** ESTO ESCRIBE EN EL KINGSTON. Va por `fwrite` + `fclose`, el camino que
`leer.bex` ya probo al leer y que solo `ESTRATOS` habia ejercitado al escribir.
Si el fichero queda a medias, el aviso lo dice con esas palabras -- "guardado A
MEDIAS" -- y entonces el numero que hay que traer es cuantos bytes tomo.

[!] El NVMe no se toca. `datos/notas.txt` esta en el volumen de datos de BMO, en
el Kingston SATA.

## T4 -- Los vatios del bloc de notas

```text
   [ ] con el bloc abierto y NADIE tocando nada: `consumo` y los vatios de la
       pinza. Tiene que parecerse al escritorio en reposo
   [ ] minimizalo y vuelve a mirar (R-APP8)
```

** La prediccion, escrita antes: **no se va a notar en la pinza.** Un bloc de
notas quieto no pinta, y lo que queda es un despertar cada 16 ms para mirar un
buzon vacio -- eso son microsegundos de CPU. Lo que esta prueba contesta no es
"cuanto ahorra" sino **que no gasta**: si con una pagina quieta suben los vatios,
el `repinta` no esta funcionando y hay un bucle pintando para nadie.

---

# 5c. UNA APP PINTA SU PROPIA IMAGEN -- `imagen.bex`

Tres piezas que existian por separado y nunca habian corrido juntas: `paquete.h`
(mi propia imagen, sin escribir ninguna ruta), `imagen.h` (el `BICO`, nuevo) y
`superficie.h`.

```text
   run c/imagen.bex
```

```text
   [ ] la consola dice: recurso de 1032 bytes, BICO de 16x16, completa=1
   [ ] se ve el rombo TRES veces: a 1x, 2x y 4x
   [ ] ** LAS CUATRO ESQUINAS DEJAN VER EL TABLERO de cuadros. Si en vez de
       eso se ve un cuadrado opaco detras del rombo, el alfa no se respeta --
       y un fondo liso lo habria escondido
   [ ] las dos de abajo estan medio fuera A PROPOSITO: media por la derecha y
       media por la izquierda. Si el recorte fallara, esto no seria un dibujo
       torcido, seria un `#PF`
```

** Y el icono que pinta es EL MISMO recurso que el escritorio pinta en la
rejilla: el dato es uno y lo leen los dos lados. Si el del escritorio se ve bien
y el de dentro no, el que miente es `imagen.h`; si los dos salen mal, miente
`Nuevo-Bico` del build.

---

# 5d. LA AZUL DEL 11-09 -- desmontando, estacion 13 (`file`)

Salio en el metal, y el kernel dijo donde estaba. Lo que se sabe, y lo que NO.

```text
   #PF   vec=0x0E  err=0x00000002   no-presente ESCRIBIENDO desde el KERNEL
   rip=0x0000000000411000
   cr2=0xFFFFFFFFFFFFFF84                 <- -124, o sea una base NULA con
                                             desplazamiento negativo
   rsp=0xFFFF800000F95AE8   pila de HILO DEL KERNEL de tid=03 (Ring 0)
   DESMONTANDO pid=02 estacion 13 file (van 1)
   ticks=0001803A
   iq: en rsp no hay marco de iretq -- el fallo NO es de un cambio de contexto
```

## Lo que es SOLIDO

`core/desmontaje.rs` no adivina: la estacion **13 es `file`**, el paso de
`obj::cap::revoke_all` que llama a `obj::file::process_died(pid)`. Y `(van 1)`
dice que **un desmontaje entero ya habia terminado bien**: revienta en el
segundo, o sea que **no es el primer intento, es la acumulacion** -- que es
justo la distincion que ese fichero se escribio para poder hacer.

El camino entero es corto y cabe aqui:

```text
   process_died(pid)   recorre las 16 ranuras; si OWNER[i] == pid -> release(i)
   release(i)          -> release_buffer(i) -> phys::free_frame() x N
                       y despues limpia OWNER/LARGO/CURSOR/WRITES/CUR/START
```

Y el disparador, dicho por el propietario: **Ctrl+Alt+ESC**. O sea que la ruta de
emergencia mata un proceso y el desmontaje se cae. Eso la hace REPRODUCIBLE, que
es la mitad de un arreglo.

## ⚠ Lo que NO cuadra, y se dice en vez de taparlo

El `rip` leido de la foto --`0x411000`-- cae **en mitad de una instruccion** en
el binario de este repo: a esa direccion `file::operation` tiene un `xorl
%eax,%eax` que ocupa `0x410FFF` y `0x411000`. Un `rip` no puede apuntar ahi.

O sea que una de estas dos es cierta, y **no se puede decidir desde el
anfitrion**:

```text
   a) una cifra esta mal leida de la fotografia (la fuente es de 8x16 y la
      pantalla estaba de lado)
   b) el kernel que se cayo NO es el de este arbol -- el despliegue anterior
      fallo en el contrato, asi que en el disco pudo quedar uno mas viejo
```

`file::release` acaba en `0x410FC1` y `operation` empieza en `0x410FD0`: los dos
son vecinos, y el desmontaje pasa por `release`. Con una cifra distinta el `rip`
cae dentro de `release`, que es donde el resto de las pruebas apunta.

## ** LA SEGUNDA AZUL LO CAMBIA TODO (misma tanda)

```text
   #PF   err=0x00000002   no-presente ESCRIBIENDO desde el KERNEL
   rip=0x0000000000411000                 <- EL MISMO
   cr2=0xFFFFFFFFFFFFFF84                 <- EL MISMO
   rsp=0x0000000000EDB058   pila baja     <- OTRA pila
   corria tid=01 (Ring 0)                 <- OTRO hilo
   DESMONTANDO pid=00 estacion 17 destroy_address_space (van 2)
   ticks=0000CEF0                         <- ANTES que la primera (0001803A)
```

Dos fallos, **el mismo `cr2` exacto**, y sin embargo:

```text
   estacion    13 `file`            17 `destroy_address_space`
   pid         02                   00
   tid         03                   01
   pila        de HILO DEL KERNEL   baja
```

*** **Entonces el fallo NO esta en `file` ni en `destroy_address_space`.** Esta
en algo que las DOS estaciones usan, y la respuesta salta sola: las dos
**devuelven marcos** -- `release_buffer` -> `free_frame`, y destruir un espacio
de direcciones libera sus tablas de pagina por el mismo sitio.

## *** EL `rip` ERA EXACTO, Y YO DIJE LO CONTRARIO (corregido el 12-09)

Escribi aqui que el `rip` mentia. **Era falso, y la correccion es el hallazgo.**

Primero lo comprobable: el `BOOTX64.EFI` del Kingston tiene el mismo SHA-256 que
el de este arbol (`95CAEFA2145C01A6...`, 11-09 20:31), o sea que **el kernel que
revienta es este**. Con eso, `0x411000` tenia que significar algo.

Y significaba. El error estaba en COMO lo mire: desensamble desde el principio de
la funcion, vi que `0x411000` caia dentro de un `xorl %eax,%eax` y conclui que el
numero estaba mal. **Lo que hay que hacer es desensamblar DESDE `0x411000`**, que
es lo que hizo el CPU:

```text
   411000: c0 48 83 c6     rorb  $0xc6, -0x7d(%rax)
   411004: fe 48 83        decb  -0x7d(%rax)
   411007: fe 07           incb  (%rdi)
```

Y encaja **entero**:

```text
   `ror` sobre memoria es LECTURA-MODIFICACION-ESCRITURA  -> err=2 (escribiendo)
   la direccion es `rax - 0x7d` = rax - 125
   con rax = 1  ->  1 - 125 = -124 = 0xFFFFFFFFFFFFFF84   -> EL cr2, clavado
```

Tres numeros independientes --el `rip`, el bit de escritura del `err` y el `cr2`
al completo-- que solo cuadran a la vez si el CPU ejecuto ESA instruccion.

*** **Asi que esto no es un puntero malo: es que el CPU SALTO A MEDIA
INSTRUCCION.** Un byte despues del principio de `xorl %eax,%eax`, en `0x410FFF`.
El "escribir a -124" no es el fallo, es lo que hace la basura que se decodifica
al entrar torcido. **El fallo es de FLUJO DE CONTROL**, y eso cambia lo que hay
que buscar: no quien escribio mal, sino QUIEN SALTO.

Y la tabla de saltos del `match` de `file::operation` --en `0x4d52c8`-- esta
LIMPIA en el binario; sus ocho entradas son `411016 41140a 4118bf 411d51 x4
411858`. Ninguna es `0x411000`. O sea que el salto torcido no sale de ahi.

## Y no es la familia del 25-08 / 30-08

Aquellas dos --`#GP` y `#PF`, las dos en `destroy_address_space`-- fallaban
**leyendo** una direccion del physmap: `HIGH_MEM_BASE + basura`, numeros
enormes. Ver `mm/vmm/amarilla.rs`, que las lleva escritas con sus restas.

Esta es al reves: **escribiendo**, y a `-124`. Eso no es una direccion del
physmap: es una **base NULA con desplazamiento negativo**, que es otra forma de
fallo y pide otro sitio donde mirar.

[!] Y `pid=00` merece una mirada propia: `NO_OWNER` vale `u32::MAX` y no cero, o
sea que el cero **no es el centinela de "sin propietario"**. Alguien pidio desmontar
el proceso numero 0.

## Lo que hay que traer para cerrarlo

```text
   [ ] repetirlo: abrir algo que use un fichero y darle a Ctrl+Alt+ESC.
       Sale SIEMPRE o a veces? Dos de dos ya lo hacen sospechoso de ser fijo
   [ ] la azul ENTERA otra vez, de frente y enfocada -- el `rip` y el `cr2`
       cifra a cifra son lo que falta
   [ ] mirar si antes de la azul salio en CABINA
       `phys: se devuelve un marco que YA estaba libre`
       -- `free_frame` lo grita y lo apunta, y si sale, el caso es un
       DOBLE FREE en el desmontaje y no un puntero nulo
   [ ] y si NO sale ese grito, el sospechoso es otro: algo escribe a traves
       de una base nula dentro de `release`
```

---

# 6. LO QUE NO SE PUEDE PEDIR TODAVIA, Y POR QUE

```text
   W6: leer los MSR `C001_0073` y `C001_0296`
       NO HAY ORDEN que los lea. Es codigo de Ring 0 y no se mete ANTES de este
       arranque a proposito: cada cambio sin verificar empeora la biseccion si
       algo falla. Se escribe DESPUES de esta hoja

   C8: la caja negra tras un reinicio EN CALIENTE
       si no se hizo el 10-09, sigue pendiente en su propia hoja
```

---

# 7. LO QUE HAY QUE TRAER DE VUELTA

Cuatro numeros y tres frases. Sin esto la tanda no cierra nada:

```text
   [ ] consumo en el ESCRITORIO quieto              ____ W
   [ ] consumo con DOOM en ventana / minimizado     ____ W  /  ____ W
   [ ] Windows quieto con HWiNFO (referencia)       ____ W
   [ ] banda                                        ____ GB/s
   [ ] el `zzz=` y la fila `bsp` de `consumo`, tal cual
   [ ] el pulso con todo quieto: `reposo` o `latido`, y el `pinta N`
   [ ] si aparecio la acusacion `[vista] ...`, con su tid
   [ ] consumo con el BLOC DE NOTAS abierto y quieto ____ W
   [ ] las tildes y la ene salen BIEN en `texto.bex`: si o no
   [ ] `datos/notas.txt` sobrevive a cerrar y abrir: si o no
   [ ] el rombo de `imagen.bex` deja ver el tablero por las esquinas: si o no
```

> Un numero que no se apunta es una tanda que hay que repetir.
