# QUE TECLEAR EN EL RYZEN -- tanda del 2026-09-13

> La regla de esta carpeta, sin cambios:
>
> *"Cada prueba dice **que afirma** y **como se cae**. Una prueba que solo puede
> salir bien no prueba nada."*
>
> Y la de orden: **lo que no toca nada va primero, lo que no se deshace va al
> final.** Hoy eso importa mas que nunca: la ultima parte puede REINICIAR el PC.

Todo lo de esta hoja compila y paso `.\bmo.ps1` el 2026-09-12. **Nada de esto lo
ha ejecutado todavia un procesador.**

```text
   .\desplegar.ps1 -Si -Arranque A -Datos A
```

---

# 1. LAS 200.000 PUERTAS -- `pulso` duerme con WAIT

La primera foto (12-09) dio los numeros buenos --4,52 GHz, 58 W, iguales que
`consumo`-- y una sonda que ensuciaba lo que media: esperaba CEDIENDO, y ceder
sin nadie mas listo vuelve en el acto. Ademas `espera_a` salia por INVOKE y no
dormia nunca (arreglado en `4e0ccdfd`).

```text
   run inti/pulso~1.ibe
```

| linea | afirma | como se cae |
|---|---|---|
| `tick` entre dos muestras | sube **~500** (medio segundo) | sube ~169: la espera sigue saliendo por su tope -- WAIT vuelve antes de tiempo en metal |
| `puertas` entre dos muestras | sube **decenas**, no miles | sube ~200.000: el `.ibx` es el viejo, o `espera_a` no cruza por WAIT |
| `mW nucl` | vuelve a **~6.800** | sigue en ~8.300: algo sigue girando |
| salida | **una linea por numero** | un churro: el `.ibx` es el viejo |

## Y el NO del jefe, en las primeras lineas

| linea | desde el ESCRITORIO | desde `run` de Ring 0 | como se cae |
|---|---|---|---|
| `inexist` | 10 | 10 | 0: el kernel es el viejo |
| `sin perm` | 3 | **0** (ese shell tiene autoridad) | 0 desde el escritorio: cualquier app ve la memoria de todas |
| `bandera` | 16 | 0 | 0 con `sin perm 3`: el motivo llega sin su bandera |

---

# 2. `bico` -- la primera herramienta de INTI

```text
   run inti/bico~1.ibe
   ls datos
```

| que | afirma | como se cae |
|---|---|---|
| consola | `bmp     0` y `qoi     0` | `1`: no encuentra `datos/foto.*` -- la ruta de metal no es la del emulador |
| | | `6`: la lectura se corto -- `LEER_EN` entrega menos en metal |
| | | la app muere: un registro alto mal codificado que el emulador no piso |
| `ls datos` | `fotob.bic` y `fotoq.bic` de **1032 B** cada uno | otro medida: la cabecera o el recuento de pixeles |

[!] Todavia no hay forma de VER un `.bic` suelto: el escritorio los pinta como
recurso dentro de un `.bex`. Esta prueba mide el fichero; mirarlo es el visor.

---

# 3. E0 -- los dos relojes de cada ventana

```text
   (abrir DOOM)            perf
   (abrir el cubo)         perf
   (DOOM minimizado ~30 s, restaurar)   perf
```

| caso | afirma | como se cae |
|---|---|---|
| DOOM a ~58 fps | `marca el paso: la APP`, `perdidos` ~0, `esperas` > `publicados` | `el DIRECTOR`: el compositor no da abasto -- o la mirada cuenta dos veces |
| el cubo | lo que salga es DATO: a 360 px deberia ser la app; en pantalla completa puede no serlo | `sin datos` con el cubo girando: `mirar` no se llama cada vuelta |
| DOOM minimizado y restaurado | `perdidos` **igual** que antes de minimizar | sube en cientos: `se_oculta` no se aplico y minimizar parece lentitud |

---

# 4. `consumo` -- la puerta ya no son 969

| fila | afirma | como se cae |
|---|---|---|
| `en puertas` | unidad **`t/vuelta`** y "675 ticks" en la nota | `c/vuelta` y "969": el `d.bex` es el viejo |
| `de un nucleo` | un ~22% MENOS ppm que la foto del 12-09 con el mismo trafico | igual: la cuenta sigue mezclando relojes |

---

# 5. *** AL FINAL, Y SOLO SI LO DE ARRIBA ESTA APUNTADO: el reinicio del cubo

El 12-09, `c/cubo.bex` + Alt+Enter **reinicio la maquina** (triple fallo en un
obrero). El despertar automatico esta apagado (`fc42f38d`); esto separa cual de
los dos caminos nuevos fue. **El primero que reinicie es el culpable y no se
sigue.**

| paso | afirma | como se cae |
|---|---|---|
| 1. `smp all` | 12 de 12 en pie, el escritorio sigue | reinicio: los obreros con MWAITX no despiertan bien |
| 2. `smp prueba` | un reparto (el 24-08 dio 11,59x) | reinicio: el reparto del kernel se rompio desde agosto |
| 3. `smp orquesta` | LLENAR sobre memoria del DIRECTOR, sin reiniciar | reinicio: el atril con obreros reales -- nunca habia corrido |
| 4. `c/cubo.bex` + Alt+Enter | `[cubo] ... escalar N us, atriles 12` | reinicio: ESCALAR con obreros |

**Tras CUALQUIER reinicio**: arrancar BMO-X directo (sin pasar por Windows) y

```text
   consumo            -> la fila `recuperado`: si NO es 0, la RAM sobrevivio
   cat datos/CAIDA.TXT
```

---

# 6. ** LOS PARCHES QUE HAY QUE CAMBIAR POR PIEZAS

El propietario: *"cambiar parches por otra pieza mejor, que siempre es asi"*. Estos son
los de esta semana, dichos por su nombre. Un parche funciona hoy y cuesta cada
vez que alguien lo toca; una pieza lo quita.

| parche que hay | por que es parche | la pieza | medida |
|---|---|---|---|
| etiquetas y rutas como numeros de 64 bits escritos a mano (`pulso`, `bico`, `cpu`), calculados con un programa aparte | un numero que nadie puede leer; cambiar `"hz real "` pide Python | INTI **pliega un texto de hasta 8 bytes a `natural64` al compilar**: `palabra("hz real ")` | chica: una fila en el descenso, y quita un paso fuera del arbol |
| `PUERTA_TICKS = 675` en `commands/reports.rs` | un numero medido COPIADO; ya caduco una vez (969) | un campo de INFO con **la ultima medida** de `ciclos.bex`, que el kernel guarde | media |
| la frecuencia y los mW se miden contra UNA lectura previa global (`cpu/power.rs`, `cpu/frequency.rs`) | dos lectores --INTI y el panel-- se roban el intervalo | el kernel da los **contadores acumulados** y cada lector resta los suyos | chica, y arregla a todos los lectores a la vez |
| `DESPERTAR_SOLO = false` en `plat/smp/atril.rs` | un interruptor que tapa un triple fallo | **GS y TSS por nucleo**: que una excepcion en un obrero sea un fallo con autopsia, no un reset. Es tambien la base del SUB-DIRECTOR | grande, y va DESPUES de la seccion 5 |
| `bico` convierte dos rutas fijas | un programa que no puede convertir otro fichero | **argumentos de programa**: hoy no existe la operacion | media |
| `_ => {}` en el `match` de coma flotante del emisor de INTI (`operaciones::flotante`) | el mismo comodin que se trago `y`/`o` y los desplazamientos | **cerrarlo con nombre**, como el entero | chica |
| el prestamo entre apps sin "a quien" | dos apps hermanas no se pueden nombrar | el **DIRECTOR presenta** a las que lanza | media, y es una decision de esquema |

** La primera y la tercera son las mas baratas y las que mas repeticion quitan.
La cuarta es la que mas vale, y la que no se toca hasta que la seccion 5 diga
cual de los dos caminos reinicio.

---

*Ver `docs/maestro/INTI_Y_LA_GPU.md` sec. 6 para E0, y `BITACORA.md` Ep. 79
para lo que paso el 12-09.*
