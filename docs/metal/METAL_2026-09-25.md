# LA 3060, EN EL METAL -- bitacora del 24-09 y tanda del 25-09

> El detalle de cada paso (que se escribio, por que, que dijo el metal) esta
> en [PLAN_LA_3060.md](../plan/PLAN_LA_3060.md). Esto es la bitacora: lo que
> salio, a que hora, y lo que hay que teclear en el siguiente arranque.

---

# 1. El 24-09: de la sonda a un triangulo del rasterizador

Un solo dia, una sola tarjeta (RTX 3060 12 GB, GA106), sin CUDA ni driver de
NVIDIA. Cada hora es la de un `save` pegado desde el Ryzen.

| hora | que salio | el paso |
|---|---|---|
| 07:10 | el GSP DESPIERTO: el booter en el SEC2 y el RISC-V del GSP activo | L0c3b `despertar` |
| 07:34 | la cola del GSP leida, sin contestar | L0c4a `cola` |
| 08:14 | 835 mensajes vaciados; detras, el secuenciador | L0c4b1 `vaciar` |
| 08:34 | SetSystemInfo y SetRegistry, leidos por el GSP | `sistema` |
| 08:59 | el secuenciador corrido: 420 de 420 ordenes | L0c4b2 `secuenciador` |
| 09:54 | `GSP_INIT_DONE`: el GSP-RM de la 570.144 corre | L0c4 `init` |
| 10:13 | la pantalla sobrevive al GSP-RM (BAR1 devuelta) | L0c4b3a `bar1` |
| 10:35 | la primera RPC: el nombre de la 3060 segun el RM | L1a `estatica` |
| 11:56 | nuestro espacio de direcciones; la CPU escribe en la VRAM | L1c1, L1c2 |
| 12:25 | nuestra raiz de paginas, dada al RM | L1c3 `directorio` |
| 12:47 | 16 paginas propias mapeadas | L1d1 `tramo` |
| 13:35 | el primer canal, atado a COPY2, con su ficha | L1d2 `canal` |
| 14:55 | **el primer trabajo**: el copiador movio 4 KiB, 1024 de 1024 | L1d3 `copia` |
| 15:51 | el contexto de oro del motor grafico, desde NUESTRA VRAM | G3, G4 `oro` |
| 16:06 | el motor grafico corre nuestro GPFIFO: semaforo pagado en 45 us | S1..S3 |
| 16:21 | **el primer sombreador**: 32 hilos, 32 de 32 | S4..S6 `sombreo` |
| 16:56 | el blur de tu escritorio y el fractal: 176 us contra 18203 (x103) | B, F |
| 18:27 | la esfera que gira y bota: 32 de 32 fotogramas | G `giro` |
| 18:35 | el rasterizador: Xid 69 (error de clase) en el primer grupo | T1c |
| 19:12 | el booter sale con 0x15 al arrancar tras una sesion | (L0c5) |
| **19:43** | **50 de 50: el triangulo del rasterizador y el de tres colores** | T1c, T2a |
| 20:36 | **el GSP APAGADO EN ORDEN**: +despedido +suspendido +sb (error 0) +descarga +wpr2-abajo, en 7 s. Despues `giro` y `raster` esperaron 1 s cada uno (la 3060 ya no trabaja) y otro `save mode` reintento `fwsec`: arreglado, ahora dicen NO al instante (motivo 82) | L0c5 |

Lo que desatasco el rasterizador: un validador de cada palabra contra
`clc797.h` (los atributos apagados llevaban un formato que no existe) y Mesa
(NVK y NAK): la SPH v4 de 128 B, el AST con el dato en su sitio, sin los
metodos reservados, y el EXIT esperando a los AST.

---

# 2. El 25-09: lo que entro y ningun metal ha visto todavia

| que | afirma | como se cae |
|---|---|---|
| **L0c5, el GSP apagado en orden** (`gpu apagar`, y `reboot` lo hace solo; ya NO es un paso de `save mode`) | la fila `apagado` ya salio bien (20:36); falta lo que importa: el arranque SIGUIENTE, sin cortar la corriente, despierta sin 0x15 | `apagado` en rojo con su motivo; o el siguiente `despertar` otra vez con 0x15 |
| **M5d P, la 3060 toma la pantalla** (`gpu pantalla`, paso `pantalla`) | el monitor ENTERO se mueve: un fractal que se acerca y vuelve, a tu resolucion; la fila dice 400 de 400 y los fps | `NO: ... BAR1 no esta en modo fisico` (falta `gpu init`); o N de 1024 muestras (escribio donde no mira el monitor) |

---

# 2b. El 25-09 en el metal: un #PF del kernel en `save mode`

`#PF err=1 cr2=0` (leer el puntero NULO, desde el kernel), `rip=0x47BA87`,
en una syscall del escritorio (`corria tid=06`, pila de SU hilo del kernel).
El `rip` NO se pudo nombrar aqui: compilado en Linux con otro nightly, esa
direccion cae en `usb::bus::bus_thread` sobre un `rdtsc` -- que no puede
fallar. El nightly no esta fijado (`channel = "nightly"`), y un `rip` solo
lo nombra el ELF que corria: `py toolchain/tools/simbolo/simbolo.py
0x47BA87` EN EL PC DONDE SE CONSTRUYO. `ticks=0x3FAE` tampoco fecha nada:
el reloj no avanza mientras una orden de la 3060 gira con las interrupciones
cerradas.

Arreglo de la pantalla, no del fallo: `plat::iommu::EN_CURSO` guarda la
orden de `TASK_OP_IOMMU` que esta corriendo, y la pantalla de fallo la dice
(`EN CURSO: la orden de la IOMMU/3060 op=0x.. arg=0x..`). La siguiente foto
dice el paso con cualquier build. Y `save mode` ARMADO ya lo sabe: al
arrancar dice `el paso X estaba EN CURSO ... TUMBO, y queda quitado`.

**Y el paso: `pantalla`.** El INFORME de antes de caer (21:24, tick 10786)
acaba en `color HECHO`; el siguiente paso es `pantalla`, y el fallo llega en
el tick 16302 (5,5 s despues). El culpable mas probable, arreglado: las 1024
muestras se leian por `display().base`, la direccion FISICA del GOP
(0xD000_0000) usada como puntero. En la syscall el CR3 es el del escritorio,
y ahi 0xD000_0000 es `vmm::FRAMEBUFFER_VA_BASE`: SU pagina de USUARIO de la
pantalla. Ring 0 leyendola con CR4.SMAP = #PF "proteccion, leyendo, desde el
kernel" (err=1). Ahora se leen por el physmap, con `clflush` (el espejo es
WB y la 3060 escribe la VRAM por fuera de la cache).

[!] Lo que no cuadra: SMAP daria `cr2=0xD0000000` y la foto dice `cr2=0`.
La proxima foto, si la hay, lo decide: la linea `EN CURSO` dira la orden.

**Visto (24-09, 21:36), con el arreglo:** `pantalla: LA 3060 PINTA TU
PANTALLA ENTERA: 8 de 8 fotogramas de 1920x1080; 250.0 fps, la 3060 en 866
us por fotograma` -- sin caida. Y detras, `apagado` otra vez entero
(+despedido +suspendido +sb +sb-bien +descarga +wpr2-abajo, error SB 0, en
7039 ms: 5 s de ellos esperando una respuesta a la despedida que no llega).

El evento de la IOMMU en la IOVA 0x20000000 NO es un fallo: es el paso
`frontera` (M0d3), que pide a proposito una direccion no prestada para ver
que la IOMMU la corta (`la FRONTERA aguanta`, = 536870912 en la caja negra).

**Y con el build del 25-09 (21:51): `save mode` entero, 51 pasos, sin
apagar.** Todo en NV_OK o PAGADO: `raster` y `color` 262144 de 262144,
`giro` 32 de 32, `pantalla` 8 de 8 a 1920x1080 (242 fps), `despertar` sin
0x15 (MAILBOX0 del SEC2 0). Y al acabar la 3060 SIGUE VIVA. Pero ese
arranque es `generacion 1` (la RAM no sobrevivio: arranque en frio), asi
que NO prueba lo de L0c5, reiniciar sin cortar la corriente: eso lo dira
un `reboot` desde BMO-X y el arranque siguiente con `generacion 2`.

**El compositor por GPU, VISTO (25-09, 04:46).** `volcado: LA 3060 LLEVO TU
ESCRITORIO A LA PANTALLA: 1024 de 1024 muestras; semaforo PAGADO; la 3060 en
649 us` (la pantalla entera, 1920x1080, en 0,65 ms: la CPU tardaba ~27 ms), y
`cada foto: POR LA 3060: 19866 tandas enviadas` -- casi veinte mil fotogramas
del escritorio llevados por el motor de copia, con DOOM a 70 fps y el cubo
girando a la vez, y `ningun fallo de Ring 3`. La fila no trae la cuenta de la
CPU porque lo corrio `save mode`, que no tiene la pantalla a mano: `gpu
volcado` a mano la da.

# 3. QUE TECLEAR en el siguiente arranque

** EL ORDEN IMPORTA. Si uno se cae: foto, `save`, y no se sigue.

(Cambiado el 25-09, tras el metal de las 21:36: `save mode` YA NO apaga el
GSP al final. Al acabar, la 3060 sigue viva para `gpu raster`, `gpu
pantalla`... Apaga `reboot`, solo, y en ~2 s, no 7.)

1. Arranca y deja que `save mode` corra (o tecleala). Son 51 pasos, el
   ultimo `pantalla`. Luego, con calma, las fotos: `gpu raster`, `gpu
   color`, `gpu giro`, `gpu pantalla` (400 fotogramas y los fps).
2. **Sin cortar la corriente**: `reboot` (NO el boton de reset: `reboot`
   apaga el GSP en orden antes; con el boton, teclea antes `gpu apagar`).
3. En el arranque nuevo, `save mode` corre otra vez. **Lo que se mira**:
   que `despertar` salga en verde, sin `MAILBOX0 del SEC2 0x15`. Si sale,
   L0c5 funciona y ya no hace falta apagar la fuente entre pruebas.

**Las imagenes para el README** (copialas a `docs/evidencia/`):

| fichero | que |
|---|---|
| `16-3060-blur.png` | el gato del fondo y, al lado, desenfocado (ya esta) |
| `17-3060-fractal.png` | el panel del fractal (ya esta) |
| `18-3060-raster.png` | el triangulo del rasterizador (`gpu raster`) |
| `19-3060-color.png` | el de tres colores (`gpu color`) |
| `20-3060-giro.png` o `.gif` | la esfera que gira (`gpu giro`) |
| `21-3060-pantalla.png` o `.mp4` | el monitor entero pintado por la 3060 (`gpu pantalla`) |

## El arranque orquestado y abrir/cerrar ventanas (25-09, tarde)

Pedido: *"dale todo animado, abrir y cerrar ventanas y splash ... mi GPU
TIENE QUE TOMAR TODO el control"*. Lo que mirar en el metal:

1. **Arranque con `save mode` ARMADO**: en vez del escritorio, un panel a
   pantalla completa: el logo BMO-X que tiembla, una barra con un trozo por
   paso (el de ahora parpadea en ambar), el paso y que hace, la BITACORA
   (`[ OK ] gsp  71 ms`) y el reloj de la CPU (TSC) al acabar cada paso.
   El panel se repinta ENTRE pasos: un paso largo (`gsp`, `despertar`) lo
   deja quieto mientras corre, porque el director es un solo hilo.
2. **Al final, LA 3060 TOMA EL CONTROL**: medio segundo de aviso en
   magenta, ~1,4 s de fractal a pantalla completa pintado por la 3060 (sin
   nada de la CPU encima), y el panel verde con cuantos fotogramas y a
   cuantos por segundo. Despues, el escritorio. **La foto buena**: el panel
   verde encima del fractal.
3. **Abrir y cerrar**: F7 o F10, o una app que nace o muere. Una linea de
   neon cian que se estira y se abre en marco (abre, 240 ms); al cerrar, lo
   mismo al reves en magenta.

La 3060 no puede pintar antes de que su GSP arranque, y eso lo hacen justo
estos pasos: la primera parte del arranque la lleva la CPU por fuerza. En
cuanto `pantalla` esta probada, manda la 3060.

**Y el gato (25-09, despues)**: del gato de la intro se pasa DIRECTO al
panel (el escritorio se prepara detras, sin verse a trozos). En el panel, el
mismo gato, en chico: DUERME en gris y la pantalla se va apagando paso a paso
mientras la 3060 no responde. Cuando `despertar` sale (el GSP responde):
**LA 3060 DESPERTO** -- el gato grande en el centro, golpe en blanco, onda
que se abre, ojos de ambar a cian y el titulo que se escribe solo (~1,1 s).
Desde ahi la rejilla brilla en neon y el gato del panel late. Sin logos de
nadie: el gato es de BMO-X. **La foto buena**: ese momento.

## La 3060 que viene caliente de Windows (25-09, 05:31 y despues)

Metal 05:31: tras usar Windows y reiniciar, la barra decia `gsp NO
desperto` y el IOMMU cazo un fallo de pagina de la 3060 (`BDF 29:00.0`,
`0x20000000`): casi seguro la tarjeta pidiendo RAM de su sesion de Windows.
La tarjeta no perdio la corriente y su GSP (o su WPR2) seguia de antes.

**Lo nuevo, en dos partes:**

1. **Se dice claro**: el consejero (`cuidado`), el globo, el aviso del final
   de `save mode` y el panel del arranque (en ambar) dicen que viene
   CALIENTE y que hacer: apagar 15 s sin corriente; desde Windows, Apagar y
   no Reiniciar (y mejor sin el "Inicio rapido" de Windows).
2. **El cargador la reinicia solo** (`faggin/s1_cpu/src/gpu_reinicio.rs`),
   ANTES de salir de la UEFI: si la 3060 llega con su GSP en marcha o la
   WPR2 arriba, para su driver UEFI, pulsa el reinicio del bus PCIe en el
   puerto de encima, restaura su configuracion, espera a que su firmware
   (GFW) arranque otra vez y se la devuelve a su driver UEFI, que enciende
   el monitor. Despues sigue el arranque de siempre. Fria: no la toca.

**Lo que mirar** (es un EXPERIMENTO: nadie sabe aun si la UEFI de esta
placa vuelve a encender la tarjeta tras el reinicio):

- Reinicia DESDE Windows y arranca BMO-X. En la pantalla de la UEFI salen
  las marcas `gpu-reset` y luego `gpu-ok` (o `gpu-nogop`).
- En BMO-X, `gpu salud`: la fila **`cargador`** dice si venia caliente, si
  se reinicio, en cuantos ms, si volvio, si su firmware arranco, si volvio
  la pantalla, y las lecturas crudas antes y despues.
- Y lo que importa: que `save mode` pase `despertar` sin el 67 ni el 0x15.

**Si el monitor se queda negro**: apaga 15 s sin corriente (la tarjeta
llega fria y el cargador no la toca), y desde Windows construye con el
interruptor apagado:

```text
   $env:BMO_GPU_REINICIO = 'no'        nunca reinicia
   $env:BMO_GPU_REINICIO = 'siempre'   reinicia incluso en frio (para probarlo a proposito)
   (sin definir)                       solo si viene caliente
   .\desplegar.ps1 -Si -Arranque A -Datos A
```

Va al compilar porque esta placa no tiene driver FAT en la UEFI: el
cargador no puede leer un fichero del ESP (todo va embebido en el shim).

---

# 4. M6 V0 -- el video por la 3060 (en codigo, 25-09)

**Hacer el fichero** (en Windows, con ffmpeg, de CUALQUIER video):

```text
   ffmpeg -i video.mp4 -t 20 -vf scale=640:360 -pix_fmt nv12 -f rawvideo clip.nv12
```

`-t 20` = 20 segundos (unos 207 MB: el NV12 va SIN comprimir). Copia
`clip.nv12` a `datos/` del disco de BMO-X.

**En BMO-X**, tras `save mode` (hace falta el canal de GR y la pantalla en
modo fisico):

```text
   gpu video datos/clip.nv12 640x360 30
```

| que | afirma | como se cae |
|---|---|---|
| el video a pantalla completa (x3), sin sonido | la fila `video` dice N de N fotogramas, los fps, y por fotograma: disco, la 3060 y comprobar | `NO: ...` con su motivo; o `N de 256 muestras` (la 3060 escribio otra cosa); o muchos `tarde` (manda el disco: la fila dice cuanto) |

---

# 5. D1 -- la 3060 bajo carga larga (en codigo, 25-09)

Despues de `save mode`, con todo en verde:

```text
   gpu aguante        2 minutos
   gpu aguante 10     10 minutos (hasta 30)
```

| que | afirma | como se cae |
|---|---|---|
| todos los trabajos que ya salieron, vuelta tras vuelta | la fila `aguante` dice N vueltas sin un fallo, por trabajo la media y la peor, y la temperatura | `FALLO <trabajo> en la vuelta N` con su motivo, y debajo los avisos del GSP: esa es la foto que hay que pegar |

Mientras corre, la pantalla la pintan `giro` y `pantalla`; al acabar se
repinta el escritorio sola.

---

# 6. `save mode` lo trae TODO solo (25-09)

Dos pasos nuevos al final, para que el modo ARMADO traiga el video y el
aguante sin teclear nada: **54 pasos**.

| paso | que hace | la fila |
|---|---|---|
| `video` | 90 fotogramas de una carta de ajuste (8 barras que corren y un cuadrado blanco), hecha en memoria, sin fichero; la 3060 la pasa a color y la agranda x3 | `video: 90 de 90 ... fps` |
| `aguante` | 20 s repitiendo todos los trabajos de la 3060, mirando la IOMMU (D3), el GSP (D4) y la temperatura | `aguante: N vueltas en 20 s, sin un fallo` |

Y `pantalla` mira ENTERO su ultimo fotograma (D2): la fila dice `D2: 1
fotograma(s) mirados ENTEROS, 2073600 de 2073600 pixeles iguales`.

---

# 7. `gpu pase` -- la burocracia una vez (P1, 25-09)

El pase de la GPU abre, se mira y se cierra; todavia NO lleva fotogramas (eso
es P2, el latido del VBLANK). Pide el VBLANK armado, porque sin latido no hay
radar y sin radar no hay pase:

```text
   gpu vblank
   gpu pase
```

| linea | afirma | como se cae |
|---|---|---|
| `ABRIR` | el lienzo prestado a la 3060 para quedarse y el buzon en `0x2_0010_0000` | `NO el pase no se abrio: <por que>` -- el por que sale de ESTADO (pantalla, IOMMU, VBLANK, lienzo, ocupado) |
| `el buzon` | MAGIA `BGPU`, su version, abierto, y la medida de TU pantalla | el kernel formo otra cosa, o mapeo otra pagina |
| `CERRAR` | el lienzo devuelto | la IOMMU no contesto la invalidacion (CABINA lo dice) |
| `la lapida` | la MISMA direccion se sigue leyendo, sin magia y con el motivo | un fallo de pagina: el escritorio se caeria -- pegar la foto |
| `ESTADO` | cerrado, "lo cerro quien lo abrio" | otro motivo: pegarlo |

Si `save mode` ya dejo el volcador armado, `gpu pase` lo suelta, prueba, y lo
vuelve a armar: `gpu volcado` tiene que seguir verde despues.

---

# 8. El informe de las 13:35 -- D2 cerraba el reloj 2 s (arreglado)

| que dijo | que era | que se hizo |
|---|---|---|
| `latido tarde 2300 ms`, "el reloj dio 2 ticks", el CPU lo tuvo `d.bex` | D2 comparaba los 2 millones de pixeles en UN syscall, y un syscall corre con las interrupciones cerradas: 2 s sin tick | `PANTALLA_FILA`: una fila por llamada; entre filas se abren |
| `pantalla` 37,7 fps, `preparar y syscall 286922 us` | los ~2 s de D2 repartidos en la cuenta de 8 fotogramas | `partido` y los fps ya no cuentan D2 |
| `aguante`: `pantalla` media 2326810 us | lo mismo: su ultimo fotograma lleva D2 | igual; el aguante sigue mirandolo entero |
| iommu `FALLO de pagina ... 0x20000000` | la FRONTERA de M0d3 (`IOVA_NO_PRESTADA`): la 3060 intenta y la IOMMU la corta. Es la prueba, no un fallo | nada |
| DOOM se cierra antes de jugar | este informe no lo vio (`lanzados 1`: solo el escritorio) | falta la foto: `cabina fallos` justo despues de que se cierre |

---

# 9. `gpu cubo` -- el cubo del estudio D3D, por la CPU (X4, 25-09)

El juez de `cubo-neutro` (del estudio de Direct3D en EPICX) traido a BMO-X.
Dibuja un fotograma del cubo por CPU y lo compara, por su huella, con lo que
D3D12 dibujo en TU 3060 bajo Windows. La historia: `docs/plan/PLAN_EL_CUBO.md`.

```text
   gpu cubo          el fotograma 30
   gpu cubo 60       otro con huella (0, 30, 60)
   gpu cubo 31       uno SIN huella: se dibuja, y dice que no hay contra que medir
```

| que | afirma | como se cae |
|---|---|---|
| el cubo centrado en la pantalla | el juez corre en el Ryzen, en el soft-float del escritorio | no se ve: la pantalla es mas chica que 1280x720 (la fila lo dice igual) |
| `IGUAL, bit a bit, a lo que D3D12 dibujo en la RTX 3060 bajo Windows` | el mismo numero en tres sistemas (Windows, Linux y BMO-X) | `DISTINTO`: pegar la huella; el primer sospechoso seria el soft-float |
| `N us de CPU` | lo que cuesta un fotograma sin SSE (en el anfitrion, ~29 ms) | -- se apunta en `datos/` como `gpu cubo us` |

Cualquier tecla devuelve el escritorio.

---

# 10. Los informes de las 15:53 y las 16:10 -- el cubo IGUAL, dos veces

| que | 15:53 | 16:10 | que dice |
|---|---|---|---|
| `gpu cubo` 30 | IGUAL, 25.066 us | IGUAL, 25.297 us | X4 cerrado, y repetible |
| `gpu cubo` 60 | IGUAL, 25.183 us | IGUAL, 25.541 us | el mismo numero que D3D12 en la 3060 |
| `gpu cubo` 31 | sin captura | sin captura, 25.137 us | se dibuja; no hay huella de Windows contra que medirlo (X6) |
| `pantalla` | 444,4 fps, partido 2250 us = 3060 807 + comprobar 1089 + preparar 354 | **463,9 fps**, partido 2155 us = 3060 795 + comprobar 1089 + preparar 271 | ahora lo que mas pesa es COMPROBAR (1024 muestras), no la 3060 |
| `video` | -- | 90 de 90, 30,0 fps, 0 tarde | igual que antes |
| `aguante` | 7 vueltas, 44 -> 50 grados | 7 vueltas en 21 s, 47 -> 54 grados, sin un evento nuevo de la IOMMU ni del GSP | la 3060 aguanta sin calentarse de mas |
| iommu | FALLO de pagina en `0x20000000` | el mismo | la FRONTERA de M0d3: es la prueba, no un fallo (seccion 8) |
| pasos | 54 de 54 | 54 de 54 | -- |

**El latido tarde que queda: 468 ms, en el ARRANQUE.** `en el tick 5396` son
~5,4 s despues de encender: no pasa mientras se usa el escritorio. `el reloj
dio 3 ticks` en 468 ms (tendrian que ser ~468) dice que casi todo ese tiempo
las interrupciones estuvieron CERRADAS, y `el CPU lo tuvo d.bex` durante 475
ms: una llamada al kernel del escritorio que tarda sin soltar, como la de D2
en la seccion 8. **Sospechoso, SIN comprobar:** los pasos que el modo ARMADO
repite al arrancar (el GSP tarda 185 ms de CORE_RESUME a GSP_INIT_DONE). Lo
que falta para saberlo: que la fila diga EN QUE syscall estaba d.bex.


---

# 11. `gpu cubo 3060` -- el cubo POR LA 3060, sin Windows (X5, 25-09)

El mismo fotograma que `gpu cubo` dibuja por CPU, dibujado ahora por el
pipeline 3D de la 3060 en una ventana de 1280x720 de la pantalla y leido de
vuelta. Pide lo mismo que `gpu raster` (el lienzo hecho) y el mapa de la
pantalla (`gpu pantalla`, o `save mode`, que ya lo deja):

```text
   gpu cubo 3060       el fotograma 30 (con huella de D3D12)
   gpu cubo 3060 60    otro con huella (0, 30, 60)
   gpu cubo 3060 45    uno sin huella: se mide contra el juez, pixel a pixel
```

| linea | afirma | como se cae |
|---|---|---|
| `fotograma N: la 3060 en X us (T triangulos); huella ...` | el dibujo se pago entero (la escalera de T1c: estado, vertices, dibujo) | `NO el cubo no se pago entero` y la escalera: el escalon sin pagar dice que metodo, como en `gpu raster` |
| `IGUAL, bit a bit, a lo que D3D12 dibujo en la RTX 3060 bajo Windows: ahora SIN Windows` | el rasterizador de la 3060 hace bajo BMO-X lo mismo que bajo Windows con el driver de NVIDIA | `DISTINTO: M pixeles` -- pegar la linea de `el primero en (x, y)` |
| `el primero en (x, y): la 3060 ..., el juez ...` | (solo si hay diferencias) el primer pixel malo | un canal con UNA unidad de diferencia: la regla 4 (el redondeo a 8 bits); la cara entera mal: el color del programa de pixel; un borde: el viewport o el centro del pixel |
| `leido de la pantalla en N ms` | las 460.800 lecturas de dos pixeles | mucho mas de un segundo: pegarlo |

Lo que se ve en la ventana es lo LEIDO de la pantalla: los pixeles de la
3060, tal cual. Cualquier tecla devuelve el escritorio.
