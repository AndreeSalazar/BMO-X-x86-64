#  modulos.toml -- que nombres trae cada `usa`.
#
#  == Las tres tablas, y por que son tres ==
#
#      comun.toml               lo que esta SIN PEDIRLO
#      modulos.toml (esta)      lo que trae `usa <modulo>`   <- REX
#      arch/<m>/inti.toml       lo que trae `usa <maquina>`  <- el metal
#
#  La linea que las separa es una sola pregunta: **lo escribe casi todo el
#  mundo?** Si si, va en `comun.toml`. Si lo escriben algunos, es un modulo y
#  hay que pedirlo. Y si ademas ata el fichero a una maquina, es una
#  arquitectura y pedirlo es declararlo.
#
#  == Lo que el compilador hace con esto ==
#
#  Saber que el nombre existe. Nada mas: los cuerpos estan en REX, que **trae el
#  cuerpo dentro** porque aqui no hay enlazado dinamico.
#
#  ** Y fijate en lo que NO cambia: `invoca` sigue sin ser palabra clave y sin
#  ser sintaxis. Es una fila de una tabla, como `outb`. La puerta del sistema
#  entra en INTI por el mismo sitio que entra una instruccion cualquiera.

[meta]
version = 1

#  La puerta: las dos llamadas congeladas del sistema.
#
#  No esta en `comun.toml` a proposito, aunque sea lo mas "de sistema" que hay:
#  **no todo programa habla con el kernel**, y uno que no lo hace no tiene por
#  que tener `invoca` ocupando un nombre.
[bmo]
invoca        = "haz esto AHORA sobre una capability; devuelve el codigo"
invoca_valor  = "la misma puerta, recogiendo el valor en vez del codigo"
espera_a      = "despiertame CUANDO pase esto"
mi_paquete    = "la capability de mi propio .bex, para leer sus recursos"

#  ** QUE RECOGE CADA UNO. No es una nota: el compilador lo lee.
#
#  La puerta de BMO-X contesta DOS cosas a la vez, por sitios distintos: un
#  CODIGO (0 = exito, y banderas del kernel en los bits altos) y un VALOR (un
#  handle, un puntero, un numero). La instruccion que se emite es la misma; lo
#  unico que cambia es de donde se lee la respuesta despues.
#
#  ** Esto vive aqui y no en la tabla de la maquina porque es del ABI de BMO-X:
#  que `invoca_valor` recoja el valor es verdad en toda maquina. DONDE esta ese
#  valor es lo que cambia, y eso si esta en `arch/<maquina>/inti.toml`.
#
#  Y sin esta seccion el fallo seria mudo: el emisor leeria el mismo registro
#  para los dos, `invoca_valor` devolveria el codigo, y todo puntero pedido al
#  kernel valdria cero -- que es indistinguible de un kernel que dice que no.
[recoge]
invoca        = "codigo"
invoca_valor  = "valor"
espera_a      = "codigo"
mi_paquete    = "valor"

#  ** POR QUE PUERTA CRUZA CADA UNO. Tampoco es una nota: el compilador lo lee.
#
#  BMO-X tiene DOS llamadas congeladas: INVOKE (haz esto) y WAIT (despiertame
#  cuando pase esto). Un nombre que no esta aqui cruza por INVOKE.
#
#  *** Esta seccion nacio de un fallo mudo (2026-09-12): el emisor cargaba el
#  numero de INVOKE para TODOS los nombres de `[bmo]`, asi que `espera_a` no
#  esperaba -- le pedia al kernel una operacion sobre el handle 0, el kernel
#  contestaba "handle invalido" y volvia en el acto. Compilaba, corria, y el
#  programa giraba creyendo que dormia. Nadie lo habia usado nunca.
#
#  Que numero es "espera" en esta maquina no se dice aqui: eso es del ABI, y el
#  emisor lo toma de `bmo_abi`, que el contrato ya compara con el kernel.
[cruza]
espera_a      = "espera"

# ===================================================================
#  ** LA MEMORIA -- leer y escribir una direccion
# ===================================================================
#
#  Sin esto, un programa de INTI puede pedirle un bloque al kernel y **no puede
#  tocarlo**. Que es exactamente donde estabamos: la puerta se abrio en F4a y al
#  otro lado no habia manos.
#
#  Es agnostico y no puede no serlo: toda maquina tiene memoria y toda maquina
#  sabe leer un byte. Lo que cambia por maquina es la instruccion, y eso lo
#  resuelve el emisor.
#
#  ** POR QUE PIDE `crudo`, con la misma regla de siempre:
#
#      al otro lado, hay alguien que comprueba?
#
#  De una direccion cruda, NO HAY NADIE. No hay kernel que valide una
#  capability como en `invoca`, y no hay comprobacion de limites como en un
#  indice de lista -- porque no hay lista: hay un numero. Escribir en la
#  direccion equivocada corrompe lo que hubiera alli y el programa sigue.
#
#  ** Y aqui esta la consecuencia bonita: el MONTON de `pleno` se va a escribir
#  en `llano`, y sus bloques `crudo` se van a CONTAR. El sitio donde nadie
#  comprueba por ti sale en el informe del .bex, con un numero, en vez de ser
#  una zona de confianza que nadie mira.
[memoria]
lee_natural8      = "el byte que hay en esa direccion"
lee_natural16     = "los dos bytes que hay en esa direccion"
lee_natural32     = "los cuatro bytes que hay en esa direccion"
lee_natural64     = "los ocho bytes que hay en esa direccion"
escribe_natural8  = "pon ese byte en esa direccion"
escribe_natural16 = "pon esos dos bytes en esa direccion"
escribe_natural32 = "pon esos cuatro bytes en esa direccion"
escribe_natural64 = "pon esos ocho bytes en esa direccion"

#  ** Los cuatro anchos estan. Los de 16 y 32 faltaron un dia, con el motivo
#  escrito: `bmo_lower` no traia los ayudantes, y escribirlos a mano dentro del
#  emisor de INTI habria puesto en un sitio lo que tiene que estar en el otro.
#
#  Se anadieron ALLI, no aqui. Y el de 32 es el que escribe un pixel en un
#  framebuffer, que era el motivo concreto para no dejarlo pendiente.

#  ** QUE HACE CADA UNO, en datos. El compilador lo lee.
#
#  `bytes` es el ancho, y esta en BYTES a proposito: "8" es verdad en toda
#  maquina, "qword" solo en una.
[accede]
lee_natural8      = { hace = "lee",     bytes = 1 }
lee_natural16     = { hace = "lee",     bytes = 2 }
lee_natural32     = { hace = "lee",     bytes = 4 }
lee_natural64     = { hace = "lee",     bytes = 8 }
escribe_natural8  = { hace = "escribe", bytes = 1 }
escribe_natural16 = { hace = "escribe", bytes = 2 }
escribe_natural32 = { hace = "escribe", bytes = 4 }
escribe_natural64 = { hace = "escribe", bytes = 8 }

# ===================================================================
#  Que pide `crudo` DE LO QUE TRAEN LOS MODULOS
# ===================================================================
#
#  ** Esta seccion afina una decision anterior, no la contradice.
#
#  `[crudo]` estuvo en `biblioteca.toml`, se mudo a `arch/<maquina>/inti.toml`
#  con este motivo escrito: *lo que exige `crudo` depende de la maquina*
#  (`entrada_puerto` existe en x86 y no en ARM).
#
#  Sigue siendo verdad, y la regla de verdad era mas general:
#
#      ** el `crudo` VIAJA CON QUIEN TRAE EL NOMBRE
#
#  La maquina trae `entrada_puerto` y trae su `crudo`. El modulo `memoria` trae
#  `escribe_natural64` y trae el suyo. Dos sitios porque hay dos fuentes de
#  nombres, no porque la regla sea distinta -- y asi un fichero sin `usa
#  memoria` no tiene que recordar que existe una prohibicion sobre un nombre
#  que no puede escribir.
[crudo]
piden = [
    "lee_natural8", "lee_natural16", "lee_natural32", "lee_natural64",
    "escribe_natural8", "escribe_natural16", "escribe_natural32",
    "escribe_natural64",
]

# ===================================================================
#  Constantes del ABI de BMO-X
# ===================================================================
#
#  Numeros que no son de ninguna maquina: son del sistema. Un programa que
#  escribiera `-2` donde va "mi propia tarea" compilaria igual y no se
#  entenderia nunca mas.
#
#  ** Van como TEXTO en hexadecimal porque un TOML guarda enteros con signo, y
#  `0xFFFFFFFFFFFFFFFE` no cabe en uno. Escribirlo como `-2` habria funcionado
#  y habria sido justo la mentira que esta seccion existe para no contar.
[constantes]
mi_tarea = "0xFFFFFFFFFFFFFFFE"

#  Las dos operaciones que el monton necesita. No estan todas las del kernel a
#  proposito: esta seccion crece cuando algo la usa, no antes. Una tabla llena
#  de numeros que nadie pide es una lista que nadie revisa.
op_pedir_memoria   = "0x15"
op_base_del_bloque = "0x01"

#  ** LA CONSOLA, que es el unico camino que un programa de `llano` tiene para
#  CONTAR algo.
#
#  En `llano` no hay `escribe`: formatear pide memoria. Lo unico que queda es la
#  puerta, y por ella caben OCHO BYTES por llamada, empaquetados y por valor.
#
#  Todo pasa por valor a proposito, y no es una limitacion temporal: es lo que
#  hace que Ring 0 no tenga que validar memoria ajena en la ruta de impresion.
#  El kernel corta en el primer byte cero.
#
#  Entra en la tabla hoy porque hoy la usa alguien --`cpu.inti`, la sonda del
#  Ryzen-- que es la regla que esta seccion se puso: crece cuando algo la pide,
#  no antes.
op_consola_escribir = "0x06"

#  ** GUARDAR UN FICHERO SIN BIBLIOTECA, que es lo que `llano` necesita.
#
#  `usa archivo` trae los nombres de REX y hoy sus llamadas NO TIENEN DESTINO:
#  hace falta enlazado, y no lo hay. Pero guardar un fichero no necesita
#  biblioteca -- necesita la PUERTA, que es lo unico que `llano` tiene:
#
#      op_ruta            acumula la ruta de 8 en 8 bytes, por valor
#      op_archivo_crear   la consume y devuelve el handle (con `invoca_valor`)
#      op_arch_escribir   sobre EL HANDLE: (n << 56) | bytes. Hasta 7 por vez
#      op_arch_cerrar     y AQUI es donde el contenido llega al disco
#
#  ** Fijate en la tercera: va sobre el handle del fichero y no sobre `mi_tarea`.
#  Es la diferencia entre pedirle algo al sistema y pedirle algo a UNA COSA que
#  el sistema te dio -- que es el modelo de capabilities entero, visto desde un
#  programa de usuario.
#
#  La ruta viaja de 8 en 8 y por VALOR por lo mismo que la consola: la superficie
#  congelada no acepta punteros, y asi Ring 0 no valida memoria ajena.
op_ruta            = "0x0B"
op_archivo_crear   = "0x11"
op_arch_escribir   = "0x02"
op_arch_cerrar     = "0x04"

#  ** LEER UN FICHERO SIN BIBLIOTECA, y de golpe (2026-09-12).
#
#  Lo pide el conversor de imagenes (`ejemplos/bico.inti`). La mitad de
#  ESCRIBIR ya estaba, y era de siete en siete bytes: para una imagen eso son
#  miles de puertas. Estas dos van por BLOQUE, igual que `fread`/`fwrite` de REX:
#
#      op_arch_leer_en      sobre el fichero: (bloque, desde, cuantos) -> leidos
#      op_arch_escribir_de  el espejo: del bloque al fichero
#
#  ** El destino no es un puntero: es la CAPABILITY del bloque y un
#  desplazamiento dentro. El kernel comprueba con una resta que cabe en lo que
#  entrego -- por eso un programa no puede pedirle que lea encima de otro.
op_archivo_abrir     = "0x10"
op_arch_medida       = "0x03"
op_arch_leer_en      = "0x06"
op_arch_escribir_de  = "0x08"

#  ** EL PERFIL EN TIEMPO REAL -- lo que el kernel ya mide y un programa puede
#  PREGUNTAR (2026-09-12).
#
#  Eddi: *"que Rust prepare la base y que INTI hable en perfil en tiempo real y
#  pueda exponer todo lo que pasa"*.
#
#  Y el reparto sale solo de la regla de siempre, sin decidir nada nuevo:
#
#      LEER un contador del silicio   Ring 0, en Rust. Desde Ring 3 es una
#                                     excepcion, no un valor lento
#      PREGUNTARLO                    la puerta, por valor, un numero por vez
#      CONTARLO                       INTI
#
#  O sea que INTI no aprende a leer esos registros: aprende a PREGUNTAR a quien
#  los lee. Y eso es mejor, no un arreglo -- el kernel resta dos lecturas y
#  divide por el tiempo, y un programa que leyera el registro crudo tendria que
#  saber hacerlo para CADA silicio.
#
#  ** El CERO es "no se sabe", nunca "no gasta". Por eso `info_sensores` va la
#  primera: dice que puede medir este silicio (bit 0 frecuencia, bit 1 consumo)
#  antes de que nadie lea un cero como si fuera un dato.
#
#  ** Frecuencia y vatios llegan como CONTADORES QUE SOLO CRECEN (2026-09-12):
#  microjulios desde el arranque, MPERF y APERF. El programa guarda su lectura
#  anterior y resta. Antes eran "desde la ultima consulta de CUALQUIERA", y dos
#  lectores --el panel y esta sonda-- se robaban el intervalo.
#
#  Numeros de `ring0/core/report.rs` y `syscall/ops.rs`. Hay una prueba que los
#  compara con el fuente del kernel: `tests/espejo_del_kernel.rs`.
op_info              = "0x13"
info_tsc_hz          = "0x05"
info_cpu_hilos       = "0x06"
info_ticks           = "0x0B"
info_smp_vivos       = "0x1B"
info_cpu_uj_paquete  = "0x69"
info_cpu_uj_nucleo   = "0x6A"
info_cpu_mperf       = "0x6B"
info_cpu_aperf       = "0x6C"
info_cpu_sensores    = "0x23"
info_puertas         = "0x2F"
#  Cuanta memoria tiene pedida cada proceso. Es de OTROS: sin autoridad, el
#  kernel dice que no. Esta aqui para que un programa pueda ver ese NO.
info_mem_quien_pid   = "0x24"

#  ** CUANDO EL JEFE DICE QUE NO, y POR QUE (L6i, 2026-09-12).
#
#  `invoca` recoge el CODIGO: los 32 bits bajos son el motivo y los altos las
#  banderas. `invoca_valor` recoge el valor, que en un NO es 0 -- por eso un
#  programa que solo mira el valor no distingue "no existe" de "vale cero", y
#  uno que mira el codigo si.
error_no_existe      = "0x0A"
error_sin_permiso    = "0x03"
bandera_falta_capability = "0x10"

#  ** PRESTAR Y TOMAR -- la memoria que viaja sin copiarse, desde INTI.
#
#  Las mismas cuatro piezas que `<bmo/prestado.h>` le da a C, y con los mismos
#  numeros: un prestamo entre un programa de C y uno de INTI no pasa por ningun
#  lenguaje, pasa por el kernel. Ninguno de los dos sabe en que esta escrito el
#  otro.
#
#      op_ofrecer        sobre TU bloque: (desde, bytes, tid de destino)
#      op_tomar          sobre `mi_tarea`: lo que haya para mi, o 0
#      op_mi_padre       el unico TID que un programa sabe sin preguntar
#      prestado_*        sobre el handle que devolvio `op_tomar`
#
#  ** OJO: prestar va a un TID CONCRETO, y hoy un programa solo conoce el de su
#  padre. Dos apps hermanas --un cubo en C y una sonda en INTI, lanzadas las dos
#  desde el escritorio-- NO se pueden nombrar la una a la otra. La punta corta
#  no es prestar: es A QUIEN. Ver `<bmo/prestado.h>`, que ya lo decia.
op_ofrecer           = "0x03"
op_tomar             = "0x1C"
op_mi_padre          = "0x26"
prestado_base        = "0x01"
prestado_bytes       = "0x02"
prestado_propietario       = "0x03"
prestado_soltar      = "0x04"

#  ** LOS ARGUMENTOS DEL PROGRAMA (2026-09-13).
#
#  `run inti/musica.ibx datos/tema.mus`: el kernel parte la linea en el primer
#  espacio y el programa lee lo de detras de 8 en 8 bytes. `0` = se acabo.
#
#  ** Y un trozo de argumento ES un trozo de ruta: para abrir el fichero que te
#  pasaron, cada palabra va derecha a `op_ruta`. Sin copiar, sin buferes.
op_argumentos        = "0x33"

#  ** EL SONIDO, sin biblioteca (2026-09-13). Lo pide el reproductor.
#
#      op_sonido_reclamar   sobre `mi_tarea`: el handle, o 0 si lo tiene otro
#      sonido_pitar         sobre el handle: (hz, ms). Hz 0 = callar ese rato.
#                           BLOQUEA y el kernel recorta a 250 ms: una nota larga
#                           se toca a trozos
#      sonido_callar        sobre el handle: ya
#      op_sonido_soltar     sobre `mi_tarea`: devolverlo sin morir
#
#  Es EXCLUSIVO, como la pantalla: dos propietarios en el mismo aparato no es
#  mezclar, es ruido.
op_sonido_reclamar   = "0x21"
op_sonido_soltar     = "0x22"
sonido_pitar         = "0x02"
sonido_callar        = "0x04"
#  ** EL TUBO del audifono USB: muestras de VERDAD, no pitidos. Todo va por el
#  primer argumento (`sub`), y el bufer es un bloque TUYO que el aparato lee
#  por DMA -- sin copias y sin puerta por muestra:
#
#      0 hay tubo?   1 armar   2 desarmar   3 bytes por trama (1 ms)
#      4 frecuencia  7 armado?  8 ofrecer (VA del bloque)  9 escrito hasta
#      10 leido      13 soltar el bufer
#
#  [!] En la MSI A320M el altavoz del PC NO suena (no hay zumbador): el tubo es
#  lo unico que se OYE.
sonido_tubo          = "0x05"

#  ** LA FORMA DE UNA SUPERFICIE (2026-09-16): lo que una app escribe en su
#  bloque para que el DIRECTOR lo componga, y el buzon por el que le llegan las
#  teclas. Son de `bmo_abi::syscalls::surface::superficie`, NO del kernel: el
#  kernel presta bytes y se aparta. Por eso `tests/espejo_del_kernel.rs` los
#  lee del ABI y no de `ring0/`.
#
#  Es el primer contrato que INTI comparte con C SIN enlazarse: C lleva los
#  mismos numeros como `BMO_SUP_*` (`tables/bmo/superficie/`), Rust como
#  `SUP_*`, y cada copia tiene su juez. Un port de la superficie a INTI (N0 de
#  `docs/plan/PLAN_NAVEGAR.md`) escribe ESTOS nombres, nunca el numero.
sup_magic            = "0x50555342"
sup_cabecera         = "0x20"
sup_bgra32           = "0x00"
sup_campo_secuencia  = "0x05"
sup_buzon_cabecera   = "0x10"
sup_buzon_ranura     = "0x08"
evento_raton         = "0x8000000000000000"
evento_letra         = "0x4000000000000000"
evento_configurar    = "0x2000000000000000"
estado_ventana       = "0x00"
estado_maximizada    = "0x01"
estado_completa      = "0x02"
sup_tomada           = "0x01000000"
vista_se_ve          = "0x00"
vista_minimizada     = "0x01"
vista_fuera          = "0x02"
vista_prestada       = "0x03"
vista_tapada         = "0x04"

#  ** LOS BITS -- el nombre es agnostico, la instruccion es de la maquina.
#
#  Peticion de Eddi (2026-08-19): *"una libreria de binarios literalmente, pero
#  usando su fortaleza para casos MUY ESPECIFICOS"*.
#
#  Y la forma correcta de hacerlo es esta, que no es obvia:
#
#      `cuenta_unos(x)` existe en TODAS las maquinas
#      en x86-64 es UNA instruccion (`popcnt`)
#      en una maquina sin ella son doce, y el programa sigue siendo el mismo
#
#  ** Por eso estos nombres viven aqui y no en `arch/x86_64/inti.toml`: lo que
#  va en la tabla de una maquina es lo que **solo existe alli** --puertos de
#  E/S, registros de control, tablas del sistema--. Contar bits no es de x86:
#  es de todas, y x86 resulta que lo hace rapido.
#
#  La diferencia se ve al portar: un programa que usa `usa binarios` se mueve; y
#  uno que usa `usa x86_64` declara que no.
#
#  ** Y hay un test que lo aprovecha: en x86-64, CADA operacion de esta lista
#  tiene una instruccion detras. Si algun dia una deja de tenerla, se sabra --
#  que es exactamente lo que hay que saber antes de escribir un bucle apretado.
[binarios]
cuenta_unos    = "cuantos bits estan a uno"
primer_uno     = "la posicion del bit mas bajo que esta a uno"
ultimo_uno     = "la del mas alto"
ceros_delante  = "cuantos ceros hay antes del primer uno, por arriba"
ceros_detras   = "cuantos hay despues, por abajo"
da_la_vuelta   = "invierte el orden de los bytes de un numero"

#  ** QUE MODULOS SE BAJAN A UNA INSTRUCCION, y no a una llamada.
#
#  Esta seccion existe por un fallo concreto: `cuenta_unos(x)` se bajaba a una
#  LLAMADA a un simbolo que no existe. Compilaba, pasaba el analisis de nombres
#  --el nombre esta en la tabla-- y el binario saltaba a la nada.
#
#  La causa no era el emisor: era que **nadie le decia al descenso que estos
#  nombres no son funciones**. Un modulo de REX trae funciones con cuerpo; este
#  trae los seis nombres de arriba, que en cada maquina son una instruccion o
#  doce, pero nunca un `call`.
#
#  ** Y por que es una FILA y no un `if nombre == "binarios"` dentro del
#  compilador: porque el dia que haya un segundo modulo asi --uno de atomicas,
#  por ejemplo-- lo que tiene que cambiar es este fichero.
[instrucciones]
son = ["binarios", "matematica"]

# ===================================================================
#  ** MATEMATICA -- lo que un motor grafico pide (2026-08-22)
# ===================================================================
#
#  Eddi: *"los motores exigen matematicas"*. Y faltaba menos de lo que parecia:
#  `+ - * /` de flotantes ya se emitian --son operadores del lenguaje-- pero la
#  raiz cuadrada **no existia por ningun nombre**. Un `ray.inti` no estaba
#  bloqueado por `pleno`: se paraba en la primera normal de un vector.
#
#  ** Y estos nombres viven AQUI y no en `arch/x86_64/inti.toml` por la misma
#  regla que trajo `binarios`, escrita unas lineas mas arriba:
#
#      lo que va en la tabla de una maquina es lo que SOLO existe alli.
#
#  Una raiz cuadrada existe en todas. En x86-64 es una instruccion (`sqrtsd`);
#  en una maquina que no la tenga seran veinte y el programa sera el mismo.
#
#  *** La primera version las puso en la tabla de x86-64 y **eso habria obligado
#  a un motor grafico a escribir `usa x86_64`**, o sea a declararse no portable
#  para hacer una raiz cuadrada. La regla ya estaba escrita; lo que fallo fue
#  leerla antes.
[matematica]
raiz    = "la raiz cuadrada de un flotante64"
minimo  = "el menor de dos flotante64"
maximo  = "el mayor de dos flotante64"
absoluto = "el valor absoluto de un flotante64"
#  ** Los BITS de un flotante, que NO son su valor. `entero64(3.5)` da 3;
#  `bits_de(3.5)` da los ocho bytes. Ver `arch/x86_64/inti.toml`.
bits_de     = "los ocho bytes de un flotante64, sin convertir su valor"
flotante_de = "el flotante64 que forman esos ocho bytes"


#  ** CON CUERPO desde el 2026-09-16 (N0 de docs/plan/PLAN_NAVEGAR.md): las
#  piezas viven en `lang/inti/runtime/entrada/` y `runtime/superficie/`, y son
#  el port de `tables/bmo/superficie/*.h` -- C es el oraculo y el contrato de
#  los numeros esta en `[constantes]` (sup_*, evento_*, vista_*).
#
#  Hasta ese dia estas dos secciones eran NOMBRES sin cuerpo: `usa superficie`
#  daba E0075 en la primera llamada. Y el nombre `mi_superficie` se fue: INTI
#  no tiene globales ("un nombre es del bloque donde nacio"), asi que la
#  superficie es un natural64 que la app guarda y pasa. `linea` y `triangulo`
#  se fueron tambien: no tienen cuerpo todavia, y un nombre sin cuerpo en esta
#  tabla es una promesa que el compilador no puede cumplir.
#
#  Las teclas y el raton llegan por el BUZON de la superficie (cero syscalls),
#  no por la entrada exclusiva de `entrada.h`, que es la del shell de Ring 0.
[entrada]
espera_tecla  = "duerme hasta que llegue una LETRA por el buzon de esa superficie, y la devuelve"
hay_tecla     = "hay algo en el buzon sin esperar? No consume"
raton         = "donde esta el puntero sobre esa ventana: x + y * 65536"

[superficie]
superficie_crear           = "pide el bloque, escribe la cabecera BSUP y se la ofrece a quien nos lanzo; 0 si nadie compone"
superficie_crear_con_buzon = "lo mismo, con un buzon de n ranuras (potencia de dos) para teclas y raton"
superficie_reconfigurar    = "contestar a un CONFIGURE: una nueva de la medida pedida con el mismo buzon"
superficie_ancho           = "el ancho que declara su cabecera"
superficie_alto            = "el alto que declara su cabecera"
superficie_pixeles         = "donde empiezan los pixeles: detras de la cabecera"
pinta                      = "el dibujo esta ENTERO: sube la secuencia, y solo entonces el DIRECTOR pinta"
limpia                     = "deja la superficie de un solo color"
pixel                      = "un pixel, o nada si esta fuera"
rectangulo                 = "un rectangulo relleno, recortado"
texto_en                   = "hasta ocho letras de una palabra en la fuente de BMO-X; devuelve la x siguiente"
superficie_evento          = "el siguiente evento del buzon, o 0"
superficie_hay_evento      = "hay algo en el buzon, sin consumirlo"
evento_hay                 = "el bit 8: es un evento"
evento_pulsada             = "el bit 9: pulsada, no soltada"
evento_es_raton            = "el bit 63: es del raton"
evento_raton_x             = "la x de un evento de raton"
evento_raton_y             = "la y de un evento de raton"
evento_botones             = "los botones de un evento de raton"
evento_es_letra            = "el bit 62: es una letra ya cocinada"
evento_letra_de            = "la letra, Latin-1"
evento_es_configurar       = "el bit 61: la ventana cambio"
configurar_ancho           = "el ancho nuevo de un CONFIGURE"
configurar_alto            = "el alto nuevo de un CONFIGURE"
configurar_estado          = "ventana, maximizada o completa"
superficie_dentro          = "el puntero esta sobre esta ventana"
superficie_puntero_x       = "la x del puntero sobre la ventana"
superficie_puntero_y       = "la y del puntero sobre la ventana"
superficie_vista           = "la VISTA: se ve, minimizada, fuera, prestada, tapada"
superficie_se_ve           = "R-APP8: si es 0, no pintes"
superficie_tomada          = "el DIRECTOR ya la tomo (bit 24)"

#  LA LAMINA: la pagina ya maquetada que manda la antena, leida y pintada
#  desde INTI (2026-09-18, N2 de PLAN_NAVEGAR). Las reglas son las de
#  `platform/shared/bmo-antena/src/lamina.rs`; aqui solo se recorren.
[lamina]
lamina_pinta    = "pinta la lamina que hay en un bloque, desplazada; 0 o codigo * 65536 + linea"
lamina_cabecera = "LAMINA ancho alto n: ancho y alto empaquetados, o NADA_MAS"
lamina_ancho    = "el ancho de una cabecera empaquetada"
lamina_alto     = "el alto de una cabecera empaquetada"
lamina_nombre   = "el nombre de un codigo de rechazo, en ocho bytes"

[archivo]
abre_para_leer    = "abre un fichero y devuelve por donde va"
abre_para_escribir = "lo mismo, para escribir"
lee_bloque        = "trae unos bytes a donde tu digas"
escribe_bloque    = "manda unos bytes"
cierra            = "suelta el fichero"

[paquete]
recurso       = "abre algo de dentro de mi propio .bex, sin cargarlo entero"
recursos      = "la lista de lo que hay dentro"

[sonido]
suena         = "un tono a una frecuencia y un tiempo"
calla         = "para el sonido"

#  == Lo que NO esta, y es una decision ==
#
#  `red`: bloqueada por el sistema, no por INTI. Entre `net rx` y algo que se
#  parezca a un socket estan ARP, IP, TCP, DNS y TLS.
#
#  Y no hay un `usa todo`. Un modulo que lo trae todo es una biblioteca comun
#  con otro nombre, y entonces la pregunta de arriba --lo escribe casi todo el
#  mundo?-- deja de tener respuesta.
