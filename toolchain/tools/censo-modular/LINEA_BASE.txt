# LINEA BASE del censo modular -- el techo de cada fichero que
# hoy incumple L6a. La regla del trinquete: un fichero de esta
# lista solo puede ENCOGER. Si crece, o si aparece uno nuevo por
# encima de 1000 lineas DE CODIGO --ni comentario ni blanco--,
# `censo_modular.py --check` dice NO. Ver `solo_codigo` para el
# por que: L6a maneja el riesgo del CODIGO, y un comentario hace
# un fichero mas facil de auditar, no mas dificil.
#
# No se edita a mano: se regenera con `--sellar` cuando un
# reparto baja un numero, y el commit muestra cuanto bajo.

[TECHOS]
  1865  toolchain/lang/cobol/emisor-x86_64/src/codegen.rs
  1562  toolchain/lang/cobol/src/parser.rs
  1064  toolchain/lang/cpp/src/parser.rs
  1107  platform/abi/bmo-abi/src/bef/validator.rs

# EXENTOS -- un "no" con motivo escrito, que se puede discutir.
# Aqui solo entra lo que NO lo escribio una persona: si la
# modularidad de un fichero la decide su fabrica, la regla se le
# aplica a la fabrica y no a lo que emite.

[EXENTOS]
toolchain/lang/c/expansion/doom/sobre/doomgeneric/d_main.c  codigo de DOOM (id Software, GPL-2.0) con los toques del port: su modularidad la decidio id, no la casa (26-09). doomgeneric_bmo.c, que SI es de la casa, sigue juzgado
toolchain/lang/c/expansion/doom/sobre/doomgeneric/g_game.c  codigo de DOOM (id Software, GPL-2.0) con los toques del port: su modularidad la decidio id, no la casa (26-09). doomgeneric_bmo.c, que SI es de la casa, sigue juzgado

# SUBIDAS -- cada techo que se levanto, con su por que. No se
# borran: una lista de excusas que se puede leer entera es lo que
# hace caro poner la siguiente.

[SUBIDAS]
Ultra_kernel_x86-64/kernel/src/ring0/syscall/mod.rs        1349 -> 1363  el despacho de KIND_TAREA (paso 3 del DIRECTOR). Se movio `conceder` y `buscar` a obj/tarea.rs y bajo de +32 a +14; los 14 no se quitan sin partir el fichero. Anotado a mano el 19-08: la subida es anterior a que esta seccion existiera.
toolchain/forge/bmo-lower/src/emu.rs                       2207 -> 2228  flags_add en el emulador: `add` usaba `flags_logic`, que pone `of` a FALSO siempre. El hueco duro meses porque NINGUN lenguaje de BMO emitia un `jo` -- C no comprueba el desbordamiento, y lo que no se emite no se emula. INTI es el primero que lo necesita (su regla 1: desbordar ATRAPA). Sin esto el emulador contesta que nunca desborda, que es la peor respuesta: HACE PASAR el test que deberia fallar. Son 21 lineas de correccion en un CAJON que ya estaba sobre la linea.
Ultra_kernel_x86-64/build.ps1                              1563 -> 1569  el SEXTO guardian (ambitos de commit): 5 lineas, y son irreducibles -- una llamada a `Guardian` son cuatro y la quinta dice donde esta el por que. [!] Y lo que esto destapa es otra cosa: build.ps1 lleva 1.568 lineas con 8 funciones (media 197) y el censo lo llama `desconocida` porque es PowerShell. Cada guardian nuevo lo engorda, asi que o se parte o esta lista tendra una entrada por guardian.
Ultra_kernel_x86-64/build.ps1                              1569 -> 1571  el helper `Guardian` distingue por fin FALTA EL SCRIPT de NO HAY PYTHON: 2 lineas. Lo primero es del repo --un path mal escrito dejo el guardian de ambitos MUERTO y el build dijo COMPLETE igual-- y lo segundo del entorno. Es literalmente el fallo que este fichero ya tenia descrito tres veces: un guardian que lee menos no avisa de menos, avisa de nada.
Ultra_kernel_x86-64/build.ps1                              1571 -> 1578  el paso que despliega la sonda de INTI: 7 lineas, y son las que hacen que `run inti/cpu.bex` exista. El .bex de cpu.inti se estaba dejando en una carpeta del repo y NUNCA LLEGABA AL FAT32 -- que es literalmente el fallo que este mismo bloque tiene escrito de cuando le paso a C: un lenguaje que compila y cuyo binario no se despliega esta a medias. Reusa `Compilar-Ejemplos`, asi que no agrega logica: una fila de tabla, un Step y la llamada. [!] Y confirma lo que la SUBIDA anterior predijo -- cada paso nuevo engorda este fichero, asi que o se parte o esta lista tendra una entrada por paso.
Ultra_kernel_x86-64/build.ps1                              1578 -> 1584  S3+S4 de INTI: el `.ibx` -- la extension deja de ser una etiqueta y pasa a ser un veredicto. [!] RECONSTRUIDO del historial el 24-08: esta subida se sello EN LOTE con el motivo de otro fichero, y ese motivo era falso aqui. Se quito y se puso el de verdad. Es el fallo que hizo que `--sellar` se niegue ahora si suben varios techos con un solo `--motivo`.
platform/abi/bmo-abi/src/bef/validator.rs                  1416 -> 1435  P1: el gate exige el perfil que viaja DENTRO del `.bex` -- 19 lineas de comprobacion en el validador. [!] RECONSTRUIDO del historial el 24-08, por lo mismo que la linea de arriba: se habia sellado con un motivo que hablaba del emulador.
toolchain/forge/bmo-lower/src/emu/mod.rs                   1689 -> 1761  AVX2: el decodificador VEX se partio a emu/vex.rs (125 lineas) y aun asi quedan +72 en mod.rs -- el campo ymm, el despacho de C4/C5 y las cinco filas de opcode, que son las que TIENEN que estar en el despachador. Partir mas seria mover el despacho fuera del despachador. [!] La subida es de un fichero de HERRAMIENTA (no corre en la maquina): bajo la regla del 24-08, si fuera de Ring 0 no habria motivo que valiera y habria que partirlo entero.
Ultra_kernel_x86-64/build.ps1                              1584 -> 1606  el guardian del contrato de syscalls pasa de leer DOS ficheros por su nombre a leer la CARPETA: 22 lineas. Se quedo ciego con el reparto de syscall/mod.rs en cinco --las operaciones que se fueron a op_*.rs dejaron de existir para el-- y la leccion estaba escrita veinte lineas mas abajo, en este mismo fichero, de la vez anterior que paso. [!] Confirma lo que las tres SUBIDAS anteriores predijeron: cada guardian engorda build.ps1, asi que o se parte o esta lista tendra una entrada por guardian.
Ultra_kernel_x86-64/build.ps1                              1534 -> 1542  el SEPTIMO guardian (las casillas de los planes): 8 lineas. [!] Y es la CUARTA vez que este mismo bloque predice lo que acaba de pasar -- 'cada guardian nuevo lo engorda, o se parte o esta lista tendra una entrada por guardian'. Ya hay CINCO entradas de build.ps1. La prediccion dejo de serlo: son 1.542 lineas de codigo con 8 funciones (media 201) y el censo lo llama 'desconocida' porque es PowerShell, o sea que ni siquiera lo juzga bien. El siguiente guardian NO se agrega: primero se parte este fichero.
